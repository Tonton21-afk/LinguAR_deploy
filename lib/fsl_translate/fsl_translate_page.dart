import 'package:flutter/material.dart';

class FSLTranslatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color(0xFFFEFFFE),
      child: Center(
        child: Text('FSL Translate Page', style: TextStyle(fontSize: 18)),
      ),
    );
  }
}
