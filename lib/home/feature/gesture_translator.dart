import 'package:flutter/material.dart';

class GestureTranslator extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gesture Translator'),
        centerTitle: true,
      ),
      body: Center(
          child: const Text(
              'Gesture Translatorafdasfasd feature content goes here')),
    );
  }
}
