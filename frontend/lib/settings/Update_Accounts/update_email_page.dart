import 'package:flutter/material.dart';

class UpdateEmailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Update Email')),
      body: Center(child: Text('Email update screen')),
    );
  }
}
